# Core scientific stack for the XAUUSD 30-minute research pipeline
numpy>=1.26,<3
pandas>=2.1,<3
scikit-learn>=1.4,<2
joblib>=1.3,<2

# CSV download helper (Yahoo is optional; broker CSVs work offline)
requests>=2.31,<3

# Existing lightweight environment support
python-dotenv>=1.0,<2

# Test runner (remove from a minimal deployment image if desired)
pytest>=8,<9
